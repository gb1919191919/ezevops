import { RefundsManager } from '@/components/refunds/RefundsManager';

export const metadata = {
  title: 'Customer Financial Ops | FleetOps',
};

export default function RefundsPage() {
  return <RefundsManager />;
}
