import { HubsDirectory } from '@/components/hubs/HubsDirectory';

export const metadata = {
  title: 'Hubs Directory | FleetOps',
};

export default function HubsPage() {
  return <HubsDirectory />;
}
