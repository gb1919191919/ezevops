import { TasksEngine } from '@/components/tasks/TasksEngine';

export const metadata = {
  title: 'Objectives & Tasks | FleetOps',
};

export default function TasksPage() {
  return <TasksEngine />;
}
